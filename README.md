# Task-7
Using python to get sales summery from SQLite Database
The notebook connects to sales_data.db using SQLite and loads data with SQL queries.
It retrieves monthly revenue by grouping sales data by year and month.
It also calculates monthly order volume using a second SQL query.
The results of both queries are displayed as pandas DataFrames.
A Matplotlib bar chart visualizes the monthly revenue trend clearly.
